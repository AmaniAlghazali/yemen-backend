const DEV_CONFIG = {
  // Change this to your computer's LAN IP when running on a physical device
  // Find it with: ifconfig | grep "inet " | grep -v 127.0.0.1
  LAN_IP: '192.168.43.58',
  PORT: 8000,
};

export default DEV_CONFIG;
